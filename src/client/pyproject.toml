[project]
name = "atlas-client"
version = "0.1.0"
description = "Atlas local TTS worker"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "kokoro>=0.9.4",
    "numpy>=2.5.1",
    "soundfile>=0.14.0",
]
